#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

double atoff(char num[]);
int main()
{
    double num, atoff(char num[]);
    char val[] = "22.2e-2";
    num = atoff(val);
    printf("%f", num);
    return 0;
}
